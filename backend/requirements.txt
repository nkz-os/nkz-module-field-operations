# =============================================================================
# nkz-module-field-operations — Python dependencies
# =============================================================================

# Core framework
fastapi>=0.109.0,<1.0
python-multipart>=0.0.9,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0

# Async HTTP client (used by cue_bridge)
httpx>=0.27.0,<1.0

# Redis async
redis>=5.0.0,<6.0

# JWT authentication
PyJWT[crypto]>=2.8.0,<3.0

# Caching
cachetools>=5.3.0,<6.0

# Database (weather data from TimescaleDB)
asyncpg>=0.29.0,<1.0

# HTTP client (used by orion_ops for direct Orion-LD calls)
requests>=2.31.0,<3.0

# MinIO client (label photo upload)
minio>=7.2.0,<8.0

# Rate limiting for public endpoints
slowapi>=0.1.9,<1.0

# FIWARE NGSI-LD client (sync operations)
nkz-platform-sdk==0.5.0

# Dev / Test
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
